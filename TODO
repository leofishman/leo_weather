- Configuration form
    - openweather api key
- Webservice service to consult openweather api
    - Ambiguity handle when more than one city name exist
    - error handle
- form to input city
- create route to show the form, the last 5 cities from localstorage, the map and the rest of the info
- leafleet maps with city coordenates
- save last 5 cities on localstorage (city, country)
- show last 5 cities on city input form, let the user delete a saved city
    - escape characters
    - stringify cities+country array to save value
    - when a city is deleted, rebuild the array and save it right then
- controller to get the saved cities clicked